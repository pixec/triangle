[http]
address = ":8000"
public_url = "http://localhost:8000"
netcat_address = ":9999"
netcat_read_timeout = "15s"

[filesystem]
root_path = "data"
# Base64-encoded 32-byte key for AES-256-GCM encryption at rest.
# Leave empty to store plaintext JSON files.
encryption_key = ""

[paste]
min_content_length = 8
max_content_length = 1000000
max_title_length = 100
id_length = 14